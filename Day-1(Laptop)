#include <iostream>
using namespace std;
int n, x, y;
int main() {
    cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> x >> y;
        if (x != y) {
            cout << "Happy Alex";
            return 0;
        }
    }
    cout << "Poor Alex";
}
