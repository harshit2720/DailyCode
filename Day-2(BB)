#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        int n,a,b;
        cin>>n>>a>>b;
        int sum=log2(n);
        cout<<(sum*a)+(b*(sum-1))<<endl;
    }
    return 0;
}
